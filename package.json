{
  "name": "mhoo-dark-factory-control-plane",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "wrangler types && tsc --noEmit",
    "build": "wrangler deploy --dry-run --containers-rollout=none",
    "test:python": "python3 -m unittest discover -s tests -v",
    "test:sandbox": "node --test tests/sandbox-agent.test.mjs",
    "test:contract": "node --test tests/d1-migration-sequence.test.mjs tests/chat-lane-registry.test.mjs tests/worker-contract.test.mjs tests/worker-dry-run-execution.test.mjs && node --experimental-strip-types --test tests/github-app-auth.test.mjs",
    "test:worker": "vitest run",
    "test": "npm run test:python && npm run test:sandbox && npm run test:contract && npm run test:worker",
    "test:coverage": "npm run test:contract && vitest run --coverage"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.9"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.3",
    "@types/node": "26.4.1",
    "@vitest/coverage-istanbul": "^4.1.11",
    "typescript": "5.9.2",
    "vitest": "^4.1.11",
    "wrangler": "4.128.0"
  }
}
