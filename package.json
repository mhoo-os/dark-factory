{
  "name": "@mhoo/dark-factory-control-plane",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler check",
    "typecheck": "wrangler types --check && tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  },
  "dependencies": {
    "@cloudflare/sandbox": "latest"
  }
}
